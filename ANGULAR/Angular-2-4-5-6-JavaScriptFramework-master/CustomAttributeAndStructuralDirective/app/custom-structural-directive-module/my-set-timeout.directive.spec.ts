import { MySetTimeoutDirective } from './my-set-timeout.directive';

describe('MySetTimeoutDirective', () => {
  it('should create an instance', () => {
    const directive = new MySetTimeoutDirective();
    expect(directive).toBeTruthy();
  });
});
