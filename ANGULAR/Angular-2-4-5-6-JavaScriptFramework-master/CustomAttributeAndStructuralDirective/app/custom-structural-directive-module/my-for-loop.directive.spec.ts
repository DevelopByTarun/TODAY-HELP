import { MyForLoopDirective } from './my-for-loop.directive';

describe('MyForLoopDirective', () => {
  it('should create an instance', () => {
    const directive = new MyForLoopDirective();
    expect(directive).toBeTruthy();
  });
});
