import { MultipleStyleDirective } from './multiple-style.directive';

describe('MultipleStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new MultipleStyleDirective();
    expect(directive).toBeTruthy();
  });
});
